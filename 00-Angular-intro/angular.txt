ng new projectname - új projekt létrehozása
ng build: kisebb verzió létrehozása

/src/app/app.html: fő html

ng s -o: elindítja az appot és megnyitja a böngészőt

ng g c: komponens létrehozása
ng g s: service létrehozása

app.html-be beirni <app-komponens neve></app-komponens neve>

komponensen belul komponens neve.html

tagen belül *ngFor = for ciklus angularban
*ngIf = feltétel

Egy tagen belül vagy csak az egyik, vagy csak a másik lehet

elemek kiíratása {{változó neve}}

ng-template: csak egy sablon, nem egyből jelenik meg, csak ha valami feltétel teljesül (lást *ngif)

[ng-Style]: feltételeket lehet szabni egy gey stílushoz, mikor legyen olyan stílus
[ng-Class]: classokra tudok feltételeket adni

ngOninit: egyből lefut ahogy betöltődik?

components
models
services

npm i bootstrap: bootstrap telepítése az angular appba

observable: adatfolyam figyelése, ha az egyikben változik, a másik is változik?
obsPigs$: Observable<PeppaInterface[]> = of(this.allPeppas);
  getPeppaPigs(): Observable<PeppaInterface[]> {
    return this.obsPigs$;
  }

Routing:
app-routing-module.ts-

Bootstrap behúzás:
npm i bootstrap
angular.json 
"styles": [
              "src/styles.css",
              "node_modules/bootstrap/dist/css/bootstrap.min.css"
            ],
            "scripts": [
              "node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"
            ],